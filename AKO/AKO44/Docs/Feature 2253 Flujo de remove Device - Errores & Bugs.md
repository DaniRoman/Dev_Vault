Cuando llega un mensaje a translator cmd para un cambio de pármetros este al enviar el `OutPutMs` comprueba si ya existe un pending con ese id y de existir actualiza la array con el cambio de parametros el problema es que si es el mismo cambio de parametro lo sigue añadiendo a la array haciendo esta interminable..

